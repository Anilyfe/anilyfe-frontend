/* ANILyfe — new file (not present in the original prototype) */

/* Manga catalog browsing — mirrors anime.js. Swap in real markup once
   there's manga data to render (see data/manga.json). */

function viewManga(){
  return comingSoonView('Manga Library', 'book-open', 'Browse manga series, volumes and box sets, and jump straight to sellers stocking them.');
}
