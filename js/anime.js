/* ANILyfe — new file (not present in the original prototype) */

/* Anime catalog browsing. The original prototype only shipped the
   marketplace + auth + profile + seller + admin views, so this is a
   scaffold: swap the body of viewAnime() for real catalog markup once
   there's anime data to render (see data/anime.json). */

function viewAnime(){
  return comingSoonView('Anime Library', 'clapperboard', 'Browse anime titles, track what you\'re watching and link them to merch in the marketplace.');
}
