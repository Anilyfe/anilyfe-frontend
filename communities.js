/* ANILyfe — new file (not present in the original prototype) */

/* Community/forum spaces for fandoms. Swap in real markup once there's
   community data to render (see data/communities.json). */

function viewCommunities(){
  return comingSoonView('Communities', 'users-round', 'Join fandom communities, share fan art and discuss your favorite series with other collectors.');
}
